语言和语言区域
===


解析语言资源所面临的挑战
---

对资源解析策略的改进
---

设计您的应用以支持附加语言区域
---
LocaleList API

从 Android 7.0（API 级别 24）开始，Android 显示的 LocaleList.getDefault() API 可让应用直接查询用户已指定的语言列表。您可以使用此 API 创建更成熟的应用行为和更优化的内容显示。

格式化程序
直到 Android 6.0（API 级别 23），Android 仅支持许多常用语言（en、es、ar、fr、ru）的一个或两个语言区域。